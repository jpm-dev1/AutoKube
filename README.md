# ⚡ AutoKube | K8's Whisperer

### 🚀 Setup & Run Instructions

#### macOS & Linux:
```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
streamlit run dashboard.py
```

#### Windows (PowerShell):
```powershell
python -m venv venv
.env\Scripts\Activate.ps1
pip install -r requirements.txt
streamlit run dashboard.py
```
